#ifndef FREQUENCIES_H
#define FREQUENCIES_H

/// Return the frequency of the given note and key
int frequency(int index, int key);

/// Return the period of the given note and key
int period(int index, int key);

#endif